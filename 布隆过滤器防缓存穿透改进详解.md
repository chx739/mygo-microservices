# 布隆过滤器防缓存穿透改进详解（方案A：Redis Bitmap 自实现）

## 1. 改进目标

本次改进针对 Trip Service 的“无效 tripID 请求穿透缓存直打 MongoDB”问题，目标是：

1. 用 Redis Bitmap 自实现布隆过滤器（无需 RedisBloom 模块）。
2. 在 `CreateTrip` 写入 Bloom 正样本。
3. 在 `GetTripByID` 查询链路中前置 Bloom 预检，miss 时快速拒绝。
4. 保持现有缓存机制兼容（Bloom 命中后仍走缓存/DB）。

---

## 2. 核心代码

## 2.1 布隆过滤器实现（共享模块）

文件：`shared/cache/bloom.go`

```go
const bloomSize int64 = 10_000_000

func BloomAdd(ctx context.Context, rdb redis.UniversalClient, filterKey, item string) error {
    h1 := bloomHash1(item)
    h2 := bloomHash2(item)

    pipe := rdb.Pipeline()
    pipe.SetBit(ctx, filterKey, h1, 1)
    pipe.SetBit(ctx, filterKey, h2, 1)
    _, err := pipe.Exec(ctx)
    return err
}

func BloomExists(ctx context.Context, rdb redis.UniversalClient, filterKey, item string) (bool, error) {
    h1 := bloomHash1(item)
    h2 := bloomHash2(item)

    pipe := rdb.Pipeline()
    bit1 := pipe.GetBit(ctx, filterKey, h1)
    bit2 := pipe.GetBit(ctx, filterKey, h2)
    if _, err := pipe.Exec(ctx); err != nil {
        return false, err
    }

    return bit1.Val() == 1 && bit2.Val() == 1, nil
}
```

关键点：

- 两个哈希函数映射两个 bit，降低误判率。
- `Pipeline` 减少网络往返。
- 输入参数做了严格校验（空 key、空 item、nil redis 都会返回错误）。

## 2.2 Trip 创建时写入 Bloom

文件：`services/trip-service/internal/service/service.go`

```go
createdTrip, err := s.repo.CreateTrip(ctx, t)
if err != nil {
    return nil, err
}

if s.rdb != nil {
    if bloomErr := cache.BloomAdd(ctx, s.rdb, tripBloomFilterKey, createdTrip.ID.Hex()); bloomErr != nil {
        log.Printf("trip bloom add failed for %s: %v", createdTrip.ID.Hex(), bloomErr)
    }
}
```

关键点：

- 下单成功后立即写入布隆过滤器，形成“正样本来源”。
- Bloom 写入失败不影响创建主流程，避免 Redis 抖动影响核心交易链路。

## 2.3 Trip 查询时 Bloom 预检

文件：`services/trip-service/internal/service/service.go`

```go
if _, objectIDErr := primitive.ObjectIDFromHex(id); objectIDErr != nil {
    return nil, fmt.Errorf("trip not found: %s", id)
}

bloomHit, bloomErr := cache.BloomExists(ctx, s.rdb, tripBloomFilterKey, id)
if bloomErr != nil {
    log.Printf("trip bloom exists check failed for %s: %v", id, bloomErr)
} else if !bloomHit {
    return nil, fmt.Errorf("trip not found: %s", id)
}

// bloom 命中后再走缓存和数据库
```

关键点：

- 非法 ObjectID 直接拒绝，减少无意义探测。
- Bloom miss 直接拦截，防止穿透到 MongoDB。
- Bloom hit 才继续缓存/DB（注意 Bloom 可能误判，所以仍要查 DB）。

---

## 3. 为什么需要实现（场景与困难）

## 3.1 场景

1. 恶意扫描
- 攻击者持续用随机 tripID 请求查询接口，导致缓存频繁 miss。

2. 上游 bug
- 某些服务 bug 产生大量错误 ID，请求持续打到 DB。

3. 高峰期资源浪费
- 明显不存在的数据仍触发 Mongo 查询，放大数据库负载。

## 3.2 困难

1. 不能引入额外 Redis 模块依赖
- 项目当前是标准 Redis 部署，不希望切换镜像。

2. 要平衡“拦截能力”和“业务可用性”
- Bloom 检查异常时不能把主链路全部阻断。

3. 要与现有缓存逻辑协同
- Bloom 是前置过滤，不应破坏已有 trip 缓存读取路径。

---

## 4. 怎么实现（技术原理）

## 4.1 布隆过滤器原理

对于任意 item，计算 $k$ 个哈希函数并将对应 bit 置 1。本实现使用 $k=2$。

查询时：

- 若任一 bit 为 0：一定不存在。
- 若全部为 1：可能存在（存在误判概率）。

## 4.2 误判与不漏判

布隆过滤器特性：

- 可能误判存在（false positive）。
- 不会误判不存在（前提是元素曾正确写入并且位图未损坏）。

因此查询链路必须是：

1. Bloom miss 直接拒绝。
2. Bloom hit 继续查缓存/DB做最终确认。

## 4.3 复杂度

- 写入：$O(k)$，本实现 $k=2$。
- 查询：$O(k)$，本实现 $k=2$。
- 空间：固定大小位图（1.25MB）。

---

## 5. 实现后提升（量化指标）

## 5.1 本次自测结果

- `go test ./shared/cache/... -v`：通过
- `go test ./services/trip-service/... -v`：通过
- `go build ./services/trip-service/...`：通过

## 5.2 建议线上观测指标

1. `trip_bloom_miss_total`
- Bloom miss 次数，反映被快速拦截的无效查询规模。

2. `trip_get_by_id_db_query_total`
- `GetTripByID` 触发 Mongo 查询次数。

3. `trip_get_by_id_db_query_reduction_ratio`
- 优化前后 DB 查询次数对比，评估防穿透效果。

4. `trip_bloom_false_positive_ratio`
- Bloom hit 但 DB miss 的比例，评估位图参数是否需要调优。

---

## 6. STAR 面试叙述

## S（Situation）
Trip 查询链路在缓存 miss 后直接回源 MongoDB，面对大量无效 tripID（恶意扫描或上游 bug）会出现缓存穿透，导致数据库压力放大。

## T（Task）
在不引入 RedisBloom 模块的前提下，实现轻量、可维护的防穿透能力，并与现有 Trip 缓存逻辑兼容。

## A（Action）
我在 `shared/cache` 自研了基于 Redis Bitmap 的布隆过滤器（双哈希），并把它接入 Trip Service：`CreateTrip` 成功后写入 Bloom 正样本，`GetTripByID` 先做 ObjectID 校验和 Bloom 预检，miss 直接拦截，hit 才走缓存/DB；同时补充单测覆盖 Bloom 行为和 service 层接入行为。

## R（Result）
实现了对无效 tripID 的快速拦截，降低缓存穿透带来的 DB 负载风险；单测与构建全部通过，方案可在现有 Redis 环境直接上线。

---

## 7. 其他技术可行性与取舍

## 7.1 为什么不用 RedisBloom 模块（方案B）
可行，但本次不采用。

原因：

- 需要 `redis-stack` 或额外模块支持，增加部署复杂度。
- 当前项目已有标准 Redis，方案A可直接复用现有基础设施。

## 7.2 为什么不用本地内存 Set
不采用。

原因：

- 多实例场景下数据不共享。
- 重启即丢失，无法稳定防穿透。

## 7.3 为什么不用“只做缓存空值”
可作为补充，不替代 Bloom。

原因：

- 空值缓存只对“已查过的 key”有效，覆盖面有限。
- Bloom 能对大规模随机 ID 提供更广覆盖。

---

## 8. 面试表达

## 8.1 简历项目表述
在 Trip Service 落地 Redis Bitmap 布隆过滤器防缓存穿透：创建行程后写入 Bloom 正样本，查询前做 Bloom 预检拦截无效 tripID，显著降低无效请求穿透 MongoDB 的风险，并完成单测与构建验证。

## 8.2 口头表述（1-2 分钟）
我们发现 Trip 查询在缓存 miss 后会直接打 MongoDB，遇到恶意扫描或错误 ID 风暴时会形成穿透。我做了一个不依赖 RedisBloom 模块的方案：在 shared 层用 Bitmap 自实现布隆过滤器，两个哈希函数映射 bit。创建订单时把 tripID 写进 Bloom，查询时先做 ObjectID 校验和 Bloom 预检，Bloom miss 直接拒绝，Bloom hit 再走缓存和数据库确认。这样既能挡住大量无效请求，也不影响真实数据查询。最后我补了 Bloom 模块和 Trip Service 的单测并跑通构建。

---

## 9. 面试高频 10 问 10 答

1. 问：布隆过滤器为什么能防穿透？
答：因为它能快速判断“某 key 一定不存在”，在访问缓存/数据库前就拦截无效请求。

2. 问：布隆过滤器有误判会不会出问题？
答：误判只会“把不存在当可能存在”，最多多查一次 DB，不会把存在的数据错判为不存在（在数据正确写入前提下）。

3. 问：为什么用两个哈希函数？
答：多个哈希位能降低碰撞和误判率，两个是成本和效果的平衡点。

4. 问：为什么选 10,000,000 bit？
答：约 1.25MB，内存开销小，结合当前规模可把误判率控制在较低水平。

5. 问：为什么 Bloom 写入失败不阻断 CreateTrip？
答：CreateTrip 是核心交易链路，Redis 短暂异常不应影响下单成功率。

6. 问：Bloom 检查失败怎么办？
答：当前实现降级继续走缓存/DB，保证可用性优先。

7. 问：只做 ObjectID 格式校验不够吗？
答：不够。攻击者完全可以构造合法格式但不存在的 ID，Bloom 用于拦截这类请求。

8. 问：如何处理历史数据不在 Bloom 的问题？
答：当前通过 DB 命中后回填 Bloom 逐步修复；生产可配离线全量预热任务。

9. 问：Bloom 需要删除吗？
答：布隆过滤器通常不支持单元素删除；若需要高动态删除可考虑计数布隆过滤器。

10. 问：怎么评估布隆效果？
答：看 `trip_bloom_miss_total`、DB 查询下降比例、Bloom 误判率等指标的前后对比。

---

## 10. 代码阅读学习顺序教程

建议按“基础能力 -> 业务接入 -> 测试验证 -> 运行验证”顺序学习：

1. 先读 Bloom 基础实现
- 文件：`shared/cache/bloom.go`
- 关注点：hash 函数、SetBit/GetBit、返回语义。

2. 再读 Trip Service 接入点
- 文件：`services/trip-service/internal/service/service.go`
- 关注点：`CreateTrip` 的 BloomAdd、`GetTripByID` 的 BloomExists 前置拦截。

3. 看 Bloom 单测
- 文件：`shared/cache/bloom_test.go`
- 关注点：add/exists 行为、参数校验边界。

4. 看 service 层缓存+布隆联合测试
- 文件：`services/trip-service/internal/service/service_cache_test.go`
- 关注点：
  - 缓存 miss/hit 流程
  - update 失效流程
  - create 写入 Bloom
  - bloom miss 短路不回源

5. 本地验证命令
```bash
go test ./shared/cache/... -v
go test ./services/trip-service/... -v
go build ./services/trip-service/...
```

按这个顺序学习，你会得到一条完整链路：

- 算法原理（Bloom）
- 工程接入（Trip Service）
- 可靠性验证（单测 + 构建）
