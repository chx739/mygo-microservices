# 其他改进（非 Redis）

> 本文只收录与 Redis 无关、但对可靠性和简历表现同样有价值的改进项。

---

## 一、WebSocket 推送链路可靠性：auto-ack -> manual ack（高优先级）

### 当前问题
`shared/messaging/queue_consumer.go` 里 `Consume(..., autoAck=true, ...)`，意味着消息一到消费者就被确认。

如果后续 `SendMessage` 失败（连接断开、瞬时网络错误），消息已被确认，无法重试，会直接丢失。

### 改进方案
将网关队列消费改成手动 ack：只有推送成功才 ack，失败时 reject（是否 requeue 由策略决定）。

```go
msgs, err := qc.rb.Channel.Consume(
    qc.queueName,
    "",
    false, // autoAck=false
    false,
    false,
    false,
    nil,
)

for msg := range msgs {
    // ... 反序列化 ...
    if err := qc.connMgr.SendMessage(userID, clientMsg); err != nil {
        // requeue=false → 消息进 DLQ（死信队列），不重回原队列
        // 本项目 shared/messaging/ 已配置 DLX，消息会到死信交换机
        // 注意：需要同时实现 DLQ 消费者，否则消息仍然"丢失在 DLQ 里"
        _ = msg.Reject(false)
        continue
    }
    _ = msg.Ack(false)
}
```

> **DLQ 策略说明**：`Reject(false)` 与 `Reject(true)` 各有取舍：
> - `Reject(false)`：进 DLQ，适合"推送失败需要人工排查或后续补推"的场景，但必须有 DLQ 消费者。
> - `Reject(true)`：重回原队列，适合"瞬时网络抖动可以立刻重试"的场景，但要配合最大重试次数防无限循环。
> 
> 本项目推荐 `Reject(true)` + 重试上限，WS 连接断开时用户通常已不在页面，重试意义不大，超过上限后再进 DLQ。

**实现难度：⭐⭐（容易）**

**简历写法**：
> 将 WebSocket 推送链路从 auto-ack 改为业务成功后手动 ack，消除“推送失败但消息已确认”的丢失窗口，提升通知可靠性。

---

## 二、Trip 状态机约束（高优先级）

### 当前问题
Trip 状态更新目前以字符串写入为主，缺少“允许转移关系”约束，容易出现非法状态跃迁。

### 改进方案
定义明确状态机，只允许白名单转移。

```go
var allowedTransitions = map[string]map[string]bool{
    "pending":  {"accepted": true, "cancelled": true},
    "accepted": {"payment_pending": true, "cancelled": true},
    "payment_pending": {"paid": true, "failed": true},
    "paid": {"completed": true},
}

func canTransit(from, to string) bool {
    next, ok := allowedTransitions[from]
    if !ok {
        return false
    }
    return next[to]
}
```

在 `UpdateTrip` 前先校验 `canTransit(current, target)`，非法转移直接返回错误。

**实现难度：⭐⭐（容易）**

**简历写法**：
> 为 Trip 引入有限状态机约束，禁止非法状态跃迁，保证核心交易流程状态一致性与可追踪性。

---

## 三、Prometheus 业务指标与告警（中优先级）

### 当前问题
项目已有 tracing（Jaeger），但缺少可用于告警和容量评估的业务指标。

### 改进方案
增加以下指标并接入 Prometheus 抓取：

- `trip_start_total`：下单请求总量
- `driver_match_latency_ms`：匹配耗时分布（Histogram）
- `payment_session_fail_total`：创建支付会话失败次数
- `ws_push_fail_total`：WebSocket 推送失败次数

```go
var wsPushFailTotal = promauto.NewCounter(prometheus.CounterOpts{
    Name: "ws_push_fail_total",
    Help: "Total failed websocket push attempts",
})

if err := qc.connMgr.SendMessage(userID, clientMsg); err != nil {
    wsPushFailTotal.Inc()
    // ... reject/ack strategy
}
```

**实现难度：⭐⭐⭐（中等）**

**简历写法**：
> 补齐核心业务指标（下单量、匹配延迟、支付失败、WS 推送失败）并接入告警，形成 tracing + metrics 的可观测闭环，缩短故障发现与定位时间。

---

## 实施优先级建议

| 优先级 | 方案 | 实现难度 | 简历价值 |
|---|---|---|---|
| P0 | WebSocket 手动 ack | ⭐⭐ | ⭐⭐⭐⭐ |
| P0 | Trip 状态机约束 | ⭐⭐ | ⭐⭐⭐⭐ |
| P1 | Prometheus 业务指标 | ⭐⭐⭐ | ⭐⭐⭐⭐ |

> **与 Redis 引入顺序无关**：本文件的三个方案均为纯业务/可靠性改动，不依赖 Redis 是否已接入，可以独立并行推进。建议在引入 Redis 的同时穿插完成 P0 两项，避免把所有改动堆到一个大 PR 里。
