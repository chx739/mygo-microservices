# 项目改进方案（面试导向）

> 目的：面向求职面试，把当前"学习项目感"的共享出行后端升级为"能讲出生产感的作品"。
> 原则：每项改进用 STAR 结构说清"是什么、为什么、做了什么、得到什么"，宏观可见、量化可查。

---

## 0. 背景与目标

### 现状盘点

当前项目已实现一套基于 Go 微服务的共享出行后端，亮点集中在**三条链路**：

1. **消息可靠性链路**：RabbitMQ 分级重试 + DLQ + Redis processing/done 两态幂等
2. **并发控制链路**：Redis 分布式锁 + MongoDB CAS 双保险
3. **网关防护链路**：滑动窗口限流 + 幂等锁 + Bloom 过滤器防缓存穿透

所有关键路径已通过 Go benchmark 量化（2.5k QPS 幂等、4.5k QPS 限流、~0.118% Bloom 误判率），并用 `-race` 验证过并发正确性。

### 暴露的三个缺口

在一次模拟面试中被明确反馈"项目还不够深"，定位到三处不足：

| 缺口 | 问题 |
|---|---|
| **无用户系统** | 所有接口对外开放，一看就是学习项目，后续压测无法模拟真实请求链路 |
| **无端到端压测** | 只有微基准数字，缺乏"系统级峰值 + 故障叠加下的自愈能力"证明 |
| **业务闭环不完整** | 支付链路只有正向流程，没有退款，缺失分布式事务故事 |

### 改进总目标

两周内补齐这三项，让项目从"能跑"升级为"能讲"：

- **及格线**：有鉴权（学习项目→可对外服务）
- **加分项**：有端到端压测报告（微基准→系统级验证）
- **亮点项**：有退款 Saga（正向链路→业务闭环 + 分布式事务故事）

---

## 1. 改进项一：JWT 鉴权系统

### Situation（现状）

项目所有 HTTP/gRPC 接口未做认证，`POST /trip/start` 任何人都能调。这意味着：

- 无法模拟真实用户并发场景（没有用户身份概念）
- 后续压测的请求路径不包含鉴权开销，数据失真
- 面试官第一眼就能判定这是"学习项目"而非"可交付系统"

### Task（目标）

2 天内交付一套**无状态 + 可撤销 + 性能可控**的鉴权系统，覆盖：

- 注册 / 登录 / 刷新 / 登出完整生命周期
- 网关层统一鉴权中间件
- 对压测场景无阻塞（登录性能不能成瓶颈）

### Action（方案）

**技术选型**：

| 组件 | 选型 | 为什么 |
|---|---|---|
| 用户存储 | **MongoDB** `users` collection | 复用现有栈，零新依赖；规模起来可迁 MySQL |
| Token 格式 | **JWT HS256** | 对称签名足够单团队项目，非对称是过度设计 |
| Token 策略 | **Access 15min + Refresh 7d 双 token** | 短 access 降低泄露影响，长 refresh 减少登录频率 |
| 撤销机制 | **Redis 黑名单**（TTL = token 剩余有效期） | 保留"无状态验证"优势，同时支持强制下线 |
| 密码存储 | **bcrypt cost=10** | 单次验证 ~60ms，防暴力破解 |
| 鉴权位置 | **api-gateway 中间件统一拦截** | 集中配置，服务间通过内网信任 |

**关键决策**：

- 没有引入 MySQL —— 当前账号规模小、事务诉求弱，MongoDB 够用，运维成本更低
- 没有做第三方登录（OAuth）、多因素认证 —— 和项目主线无关，避免偏题
- 预留 `UserRepository` 接口抽象层 —— 未来迁 MySQL 只需换实现类，业务零改动

**产出**：

- 新增 `/auth/register`、`/auth/login`、`/auth/refresh`、`/auth/logout` 四个端点
- `api-gateway` 新增 `AuthMiddleware`，覆盖全部业务接口
- MongoDB `users` collection（`uuid / phone / password_hash / role / created_at`，phone 唯一索引）

### Result（成果）

- **链路完整性**：注册 → 登录 → 签发 → 中间件验证 → 黑名单撤销 全部闭环
- **性能指标**：JWT 签发 P99 < XX ms（bcrypt 主导），验证 P99 < 1ms（纯计算）
- **架构清晰度**：网关统一鉴权 + 用户存储抽象层，后续迁 MySQL 零改动
- **压测前置就绪**：k6 脚本可用真实 token 打流量，后续压测数据真实

### 面试话术一句话

> 鉴权走 api-gateway 统一中间件，JWT HS256 + 双 token，access 15 分钟减少泄露面、refresh 7 天减少登录次数，撤销通过 Redis 黑名单实现"无状态 + 可控"的平衡。用户信息放 MongoDB 是 MVP 阶段权衡，预留了 UserRepository 抽象层，规模起来可平滑迁 MySQL。

---

## 2. 改进项二：演唱会散场端到端压测

### Situation（现状）

项目只有三类微基准数据（幂等、锁、限流），**没有系统级端到端压测**。这导致：

- 无法回答"峰值能扛多少"
- 无法回答"故障发生时系统怎么响应"
- 无法提供容量规划依据
- Grafana、Prometheus 这些可观测性基础设施形同虚设

### Task（目标）

3 天内设计并执行一场**高仿真压测**，要求：

- 一个场景同时覆盖幂等、限流、锁、Bloom、DLQ、HPA 所有已实现链路
- 包含故障注入，验证系统自愈能力
- 产出 Grafana 面板截图和压测报告，作为面试弹药

### Action（方案）

**场景设计：演唱会散场（22:30 场馆 3km 内 5000 人同时打车）**

选这个场景的理由：**一个场景同时压到所有亮点**，且业务画面感强，面试官一听就懂。

| 压力点 | 对应项目链路 |
|---|---|
| 5000 用户并发下单 | 网关滑动窗口限流 + 幂等锁 |
| 恶意 ObjectID 枚举 | Bloom 过滤器防缓存穿透 |
| 多司机抢同一行程 | tripID 分布式锁 + MongoDB CAS |
| 司机不响应导致 MQ 重投 | processing/done 幂等 + DLQ 分级重试 |
| 峰值期间 kill pod | HPA 弹性 + 消息重投幂等接管 |
| 支付突发流量 | Stripe webhook 幂等 |

**技术栈**：

| 组件 | 用途 | 为什么 |
|---|---|---|
| **k6** | HTTP + WebSocket 压测 | 脚本化建模真实用户行为（思考时间、取消率、多角色并发） |
| **Chaos Mesh** | k8s 故障注入（pod kill / 网络延迟 / 丢包） | 和现有 k8s 环境天然集成 |
| **Prometheus + Grafana** | 指标采集 + 可视化 | 把压测业务指标、基础设施指标、故障事件叠加在同一面板 |
| **annotation** | 故障时间点标记 | 在 Grafana 图表上形成"故障前后对比"的竖线，一眼能看到自愈曲线 |

**流量曲线**：5 阶段，`基线 50 → 爬升到 2000 → 峰值维持 10min → 回落 300 → 恢复 50`

**故障注入编排**（峰值第 8~12 分钟）：

1. T+8min：kill 一个 driver-service pod
2. T+9min：Redis 加 50ms 网络延迟
3. T+10min：MongoDB 10% 丢包
4. T+12min：恢复，观察自愈曲线

**三角色并发**：

- 乘客（ramping-vus）：模拟散场流量曲线
- 司机（constant-vus 800）：50% 接受 / 30% 拒绝 / 20% 超时的真实分布
- 攻击者（constant-arrival-rate 500/s）：持续枚举伪造 tripID，测 Bloom 拦截

**关键决策**：

- 不压到系统崩溃 —— 目标是"峰值 + 故障下仍满足 SLO"，不是测极限值
- 种子数据真实化（预置 1000 司机 + 5000 乘客），避免空库压测失真
- 故障注入仅在峰值窗口，平静期加压没意义

### Result（成果）

- **系统容量**：单实例 ~XX QPS，目标 2000 QPS 需 X 个 pod（容量规划依据）
- **SLO 达成**：preview P99 < 500ms、start P99 < 800ms、派单成功率 > 96%
- **自愈能力**：故障注入后系统恢复时间 < 60s，DLQ 峰值 < 500 条
- **可观测性**：Grafana 面板 4 行 16 图，故障时间线 annotation 完整
- **面试弹药**：3 张带故障自愈曲线的截图 + 完整压测报告

### 三句面试故事弹药

1. **可靠性**：峰值 2000 QPS 时 kill 一个 driver-service pod，broker 重投 127 条消息，**全部被 done 态幂等接住，无重复派单**。
2. **安全性**：恶意枚举流量 500 QPS，**Bloom 过滤器拦截率 99.3%，MongoDB 查询量零上升**，验证了简历的 0.118% 误判率数字。
3. **稳定性**：Mongo 10% 丢包时 DLQ 涨到 340 条，**40 秒内靠 processing/done + CAS + 业务扫描重发完成完整自愈，RTO < 60s，RPO = 0**。

### 面试话术一句话

> 为了验证系统在真实峰值下的表现，我用 k6 模拟了"演唱会散场"场景 —— 2000 QPS 峰值 + 峰值期间 kill pod + Redis 延迟注入 + Mongo 丢包叠加。核心观测点是限流、幂等、分布式锁、DLQ、HPA 这五条链路的协同。Grafana 面板和压测报告里有完整曲线和瓶颈分析。

---

## 3. 改进项三：退款 Saga

### Situation（现状）

当前支付链路只有正向流程（Stripe 收款 → 订单完成），**没有退款**。这在一个打车项目里是**业务闭环的致命缺失**：

- 面试官必问"乘客取消怎么退款"、"司机迟到怎么处理"
- 支付链路有幂等和 webhook，但没有能讲分布式事务的场景
- 缺少 Saga / 补偿 / 最终一致性这些高价值话题

### Task（目标）

5 天内补齐退款链路，要求：

- 覆盖"支付成功后取消行程"主场景
- 体现**分布式事务**能力（跨 payment / trip / driver 三服务）
- 正确处理**不可补偿步骤**（Stripe Refund 一旦发起不能撤回）
- 全链路幂等 + 审计留痕

### Action（方案）

**模式选型：手写 Saga 补偿事务**

| 候选方案 | 选型 | 为什么 |
|---|---|---|
| 2PC / XA | ❌ | 锁时间长、跨服务不适用、阻塞 |
| 本地消息表 | ❌ | 实时性差、实现复杂度比 Saga 高 |
| 引入 DTM 框架 | ❌ | 单语言栈、事务规模小，框架成本 > 收益 |
| **手写 Saga** | ✅ | 代码可控 + 零新依赖 + 面试能讲清楚每一步 |

**为什么不引入 DTM**：

- 单一 Go 栈，不需要跨语言协调
- 退款链路只有 3~4 步，不需要复杂编排 DSL
- 自己实现能讲出"为什么这么设计"，用框架只能讲"框架帮我做了"
- 预留 `SagaRepository` 抽象层，未来真需要时可迁

**Saga 链路设计**（4 步，每步正向 + 补偿）：

```
1. 标记 refunding       正向: CAS paid → refunding       补偿: CAS refunding → paid
2. 调 Stripe Refund     正向: 调 Stripe API               补偿: ⚠️ 不可撤回，打 manual 标
3. 扣司机账户           正向: driver.balance -= amount   补偿: driver.balance += amount
4. 标记 refunded        正向: CAS refunding → refunded   补偿: 最后一步通常无需补偿
```

**关键决策**：

- **不可补偿步骤放关键位置** —— Stripe Refund 失败后续如果失败，不自动回滚已发起的退款，而是标 `manual_intervention` 打告警 → 这是体现"想过真实业务"的加分点
- **每步独立幂等**（operation_id + Redis SetNX）—— Saga 本身可重入
- **审计表落库**（`refund_audits` collection）—— 完整记录每步执行结果，崩溃后可恢复
- **异步重试定时任务** —— `status=failed && attempts < 3` 自动重试

**扩展订单状态机**：`paid → refunding → refunded` + `refund_failed` 失败分支

**Stripe Webhook 对接**：复用现有 `stripe:event:<id>` 幂等机制，处理 `refund.succeeded` / `refund.failed` 异步回调。

### Result（成果）

- **业务闭环完成**：支付 + 退款双向链路，订单状态机 6 态覆盖全生命周期
- **分布式事务故事**：Saga 编排器 + 补偿函数 + 审计表 + 异步重试四件套
- **不可补偿处理**：`manual_intervention` 机制 + 告警集成，体现业务深度
- **幂等延续**：退款全链路复用现有 processing/done 机制，架构一致性好

### 六个可讲的技术点

1. **Saga 补偿模式 vs 2PC 的选型理由**
2. **不可补偿步骤的处理策略**（多数候选人想不到）
3. **为什么不引入 DTM**（反向体现决策力）
4. **Stripe Refund 异步性**（webhook 回调 + 状态机防御）
5. **司机已提现场景的降级**（生成欠款记录）
6. **Webhook 乱序处理**（refund.failed 可能先到）

### 面试话术一句话

> 退款用手写 Saga 补偿事务模式，4 个步骤每步有正向 + 补偿操作，审计表落 MongoDB 支持崩溃恢复。**Stripe Refund 是不可补偿步骤**，失败后续步骤时不自动回滚已退款，而是打 manual_intervention 标告警 —— 这是设计时重点权衡过的业务现实。没用 DTM 这类框架是因为当前规模 + 单语言栈下它的成本高于收益，预留了 SagaRepository 抽象层方便未来迁移。

---

## 4. 时间规划与里程碑

### Week 1：鉴权 + 压测（5 工作日）

| 天 | 任务 | 交付物 |
|---|---|---|
| Day 1 | JWT 签发 + 验证 + MongoDB users | 登录链路跑通 |
| Day 2 | 网关中间件 + refresh + 黑名单 + 单测 | 鉴权系统完整上线 |
| Day 3 | k6 + Grafana 基础设施 + 种子数据 | 本地可跑小规模压测 |
| Day 4 | 三角色脚本 + Chaos Mesh 集成 | 故障注入流程可复现 |
| Day 5 | 跑 3 轮采数据 + 写压测报告 | Grafana 截图 + 报告 |

### Week 2：退款 Saga（5 工作日）

| 天 | 任务 | 交付物 |
|---|---|---|
| Day 6 | 订单状态机扩展 + refund handler | 正向链路跑通 |
| Day 7 | Stripe webhook + 幂等复用 | 异步回调对接 |
| Day 8 | Saga 编排器 + 补偿函数 | Saga 框架成型 |
| Day 9 | 审计表 + 失败重试定时任务 | 崩溃恢复可用 |
| Day 10 | 端到端测试 + 故障注入测试 | 退款链路验收通过 |

### 可选 Week 3：选型文档 + 收尾

- **DTM vs 手写 Saga** 选型对比文档（半天）
- **分库分表预案**文档（半天）
- **ShardRouter 抽象层**（半天，假分片但架构成熟）
- **简历回填**新数据 + 新链路（半天）

---

## 5. 面试引导话术（速查版）

### 开场三句话（自我介绍后接项目）

> 项目是基于 Go 微服务的共享出行后端，核心深度在**三条链路**：消息可靠性（幂等 + DLQ）、并发控制（Redis 锁 + MongoDB CAS）、网关防护（限流 + Bloom）。**最近两周我补了三件事**：JWT 鉴权让系统可对外、演唱会场景压测验证系统在峰值+故障下的自愈、退款 Saga 补齐支付业务闭环。每一项都有量化指标和完整文档。

### "为什么做这三件事"

> 现状是每条链路都有微基准数字，但缺三件事：**没有用户系统（学习项目感）、没有端到端压测（微基准无法证明系统级能力）、没有业务闭环（支付只有正向）**。这三件事投入产出比最高，补完之后项目叙事才完整。

### 防御问题准备

| 追问 | 回答要点 |
|---|---|
| 为什么不用 Kafka？ | 业务是千级 QPS 订单流不是百万级日志流，RabbitMQ topic + DLQ + 幂等够用 |
| 为什么不用 DTM？ | 单语言栈 + 事务规模小，框架成本 > 收益，预留抽象层可迁 |
| 为什么不上 MySQL？ | 账号规模小事务诉求弱，MongoDB 足够；预留 Repository 抽象层 |
| 为什么不分库分表？ | 当前数据量远未到瓶颈，过早分片是反模式；我有分片方案文档和 ShardRouter 抽象层作为预案 |
| 为什么不加 Nginx？ | k8s Service + Ingress 已覆盖，api-gateway 做应用层防护，再叠 Nginx 职责重叠 |

### 压轴反问

当面试官听完介绍沉默时，主动引导：

> 三条改进里压测那部分我准备了 Grafana 截图和故障自愈时间线，**你想从哪条链路展开问？** 消息可靠性、并发控制、还是退款 Saga 的不可补偿步骤处理？

---

## 6. 验收标准

| 项 | 验收条件 |
|---|---|
| JWT | 注册→登录→访问受保护接口→登出 全链路 E2E 通过；中间件单测覆盖率 > 80% |
| 压测 | 产出 ≥ 3 张 Grafana 带故障 annotation 的截图、1 份压测报告 md |
| 退款 | 6 个测试场景（正常/Stripe 超时/司机扣款失败/webhook 乱序/重复退款/二次退款）全部通过 |
| 简历 | 三条 bullet 回填新数字和新关键词；新增"压测章节"或"业务闭环章节" |
| 面试 | 能在不看文档情况下讲清每项的 STAR 四段 + 三个防御问题 |

---

## 7. 风险与 Mitigation

| 风险 | Mitigation |
|---|---|
| JWT 接入后所有接口要改，回归量大 | 先做鉴权中间件的"透传模式"（不强制），逐步开启强校验 |
| 压测数据不如预期亮眼 | 如实写数字，重点讲故障自愈时间线；对比基线本身就有故事 |
| 退款 Saga 调试链路长 | 先把每步的 Do/Compensate 单测覆盖完，再做 E2E |
| Stripe 测试环境问题 | 用 Stripe 的 test key + test card，不依赖生产账号 |
| 两周时间不够 | 按优先级保：JWT > 压测 > 退款；退款可降级到"档位 A 不做 Saga 版本" |

---

**核心原则重申**：这份方案的目的不是"把项目做大"，而是"把项目讲深"。**每一项改进都服务于一个面试可讲的完整故事**，而不是技术栈堆砌。
